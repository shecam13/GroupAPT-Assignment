@model APTEventAssignment.ViewModels.EventBookingSeatsViewModel

@{
    ViewBag.Title = "SeatingPage";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <h2>SeatingPage</h2>

    @using (Html.BeginForm("SeatingPage", "EventBookingSeats", FormMethod.Post, new { role = "form" })) //redirects back to the controller to handle post request
    {
        @Html.AntiForgeryToken()
        
        <div class="form-group">
            <br>
            @Html.HiddenFor(x => x.Event_Name)
            @Html.HiddenFor(x => x.Event_VenueName)
            @*<p>Choose date:  @Html.DropDownListFor(model => model.SelectPerformanceId, Model.Performances)</p>*@
            <p>Choose date:  @Html.DropDownListFor(model => model.SelectPerformanceId, Model.Performances)</p>
            @*@Html.DropDownListFor(model => model.SelectPerformanceId, Model.Performances, "Choose... ")*@
            <hr>
            
            <div class="form-group">
                <div class="row">
                <div class="col-md-3">
                    <label class="control-label">Enter mobile number <i>(Optional)</i> : </label>                   
                </div>
                <div class="col-md-4">
                    @Html.TextBoxFor(m => m.PhoneNumber, new { @class = "form-control" })
                </div>
                </div>
           </div>
           <hr>
        </div>

        @*@foreach (var item in (List<VenueZone>) ViewData["venueZones"])
        {
            @item.VenueZone_Rows;
        }*@

   
    
        @*<iframe src="@Url.Action("ViewSeatingPlan", "EventBookingSeats")" width="1000" height="400"></iframe>*@
    <div class="form-group">
        @*<input type='submit' class="btn btn-primary" value='Book Tickets' onclick='window.location = "@Url.Action("IndexBooking", "EventBookings")";' />*@
        <input type='submit' class="btn btn-primary" value='Book Tickets' />
        
    </div>
            @*<input type='submit' class="btn btn-primary" value='Book Tickets' />*@
    }
    </div>