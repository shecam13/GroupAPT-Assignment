@model IEnumerable<APTEventAssignment.Models.VenueType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VenueType_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VenueType_Deleted)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VenueType_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VenueType_Deleted)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VenueType_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VenueType_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VenueType_ID })
        </td>
    </tr>
}

</table>
